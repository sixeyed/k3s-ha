apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-nfs.fullname" . }}
  labels:
    {{- include "postgres-nfs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "postgres-nfs.selectorLabels" . | nindent 4 }}