apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-local.fullname" . }}
  labels:
    {{- include "redis-local.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "redis-local.selectorLabels" . | nindent 4 }}